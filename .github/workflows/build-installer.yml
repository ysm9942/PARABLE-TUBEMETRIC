name: Build Local Agent Installer

on:
  push:
    branches: [main]
    paths:
      - "installer/**"
      - "backend/**"
      - "scraper/**"
  workflow_dispatch:   # 수동 실행 가능

jobs:
  # ─── Windows .exe 인스톨러 빌드 ───────────────────────────────────────────
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: pip 패키지 설치
        run: |
          pip install pyinstaller
          pip install -r backend/requirements.txt

      - name: PyInstaller 빌드
        working-directory: installer
        run: pyinstaller tubemetric.spec --clean

      - name: Inno Setup 인스톨러 빌드
        uses: Minionguyjpro/Inno-Setup-Action@v1.2.2
        with:
          path: installer/setup.iss
          options: /O"installer/Output"

      - name: 아티팩트 업로드
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer
          path: installer/Output/TubeMetric-Agent-Setup-Windows.exe
          retention-days: 7

  # ─── macOS .pkg 인스톨러 빌드 ─────────────────────────────────────────────
  build-macos:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: pip 패키지 설치
        env:
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: "1"
        run: |
          pip install pyinstaller
          pip install -r backend/requirements.txt

      - name: macOS PKG 빌드
        timeout-minutes: 20
        working-directory: installer
        run: |
          chmod +x build_macos.sh
          ./build_macos.sh

      - name: 아티팩트 업로드
        uses: actions/upload-artifact@v4
        with:
          name: macos-installer
          path: installer/dist/TubeMetric-Agent-Setup-macOS.pkg
          retention-days: 7

  # ─── SoftC Scraper Windows .exe 빌드 ─────────────────────────────────────
  build-softc-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: pip 패키지 설치
        run: |
          pip install pyinstaller
          pip install fastapi uvicorn[standard] pydantic undetected-chromedriver selenium beautifulsoup4 lxml

      - name: PyInstaller 빌드
        working-directory: installer
        run: pyinstaller softc_scraper.spec --clean

      - name: Inno Setup 인스톨러 빌드
        uses: Minionguyjpro/Inno-Setup-Action@v1.2.2
        with:
          path: installer/setup_softc.iss
          options: /O"installer/Output"

      - name: 아티팩트 업로드
        uses: actions/upload-artifact@v4
        with:
          name: softc-windows-installer
          path: installer/Output/TubeMetric-SoftC-Scraper-Setup-Windows.exe
          retention-days: 7

  # ─── SoftC Scraper macOS .pkg 빌드 ────────────────────────────────────────
  build-softc-macos:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: pip 패키지 설치
        run: |
          pip install pyinstaller
          pip install fastapi uvicorn[standard] pydantic undetected-chromedriver selenium beautifulsoup4 lxml

      - name: macOS PKG 빌드
        timeout-minutes: 20
        working-directory: installer
        run: |
          chmod +x build_softc_macos.sh
          ./build_softc_macos.sh

      - name: 아티팩트 업로드
        uses: actions/upload-artifact@v4
        with:
          name: softc-macos-installer
          path: installer/dist/TubeMetric-SoftC-Scraper-Setup-macOS.pkg
          retention-days: 7

  # ─── Instagram Scraper Windows .exe 빌드 ────────────────────────────────
  build-instagram-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: pip 패키지 설치
        run: |
          pip install pyinstaller
          pip install fastapi uvicorn[standard] pydantic undetected-chromedriver selenium python-dotenv

      - name: PyInstaller 빌드
        working-directory: installer
        run: pyinstaller instagram_scraper.spec --clean

      - name: Inno Setup 인스톨러 빌드
        uses: Minionguyjpro/Inno-Setup-Action@v1.2.2
        with:
          path: installer/setup_instagram.iss
          options: /O"installer/Output"

      - name: 아티팩트 업로드
        uses: actions/upload-artifact@v4
        with:
          name: instagram-windows-installer
          path: installer/Output/TubeMetric-Instagram-Agent-Setup-Windows.exe
          retention-days: 7

  # ─── Instagram Scraper macOS .pkg 빌드 ───────────────────────────────────
  build-instagram-macos:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: pip 패키지 설치
        run: |
          pip install pyinstaller
          pip install fastapi uvicorn[standard] pydantic undetected-chromedriver selenium python-dotenv

      - name: macOS PKG 빌드
        timeout-minutes: 20
        working-directory: installer
        run: |
          chmod +x build_instagram_macos.sh
          ./build_instagram_macos.sh

      - name: 아티팩트 업로드
        uses: actions/upload-artifact@v4
        with:
          name: instagram-macos-installer
          path: installer/dist/TubeMetric-Instagram-Agent-Setup-macOS.pkg
          retention-days: 7

  # ─── 전체 통합 에이전트 Windows 빌드 (라이브 + Instagram·TikTok) ─────────
  build-all-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: pip 패키지 설치 (전체)
        run: |
          pip install pyinstaller
          pip install -r backend/requirements.txt
          pip install fastapi uvicorn[standard] pydantic undetected-chromedriver selenium python-dotenv yt-dlp

      - name: PyInstaller 빌드 — tubemetric-agent
        working-directory: installer
        run: pyinstaller tubemetric.spec --clean

      - name: PyInstaller 빌드 — instagram-scraper (Instagram + TikTok)
        working-directory: installer
        run: pyinstaller instagram_scraper.spec --clean

      - name: Inno Setup 통합 인스톨러 빌드
        uses: Minionguyjpro/Inno-Setup-Action@v1.2.2
        with:
          path: installer/setup_all.iss
          options: /O"installer/Output"

      - name: 아티팩트 업로드
        uses: actions/upload-artifact@v4
        with:
          name: all-windows-installer
          path: installer/Output/TubeMetric-All-Agents-Setup-Windows.exe
          retention-days: 7

  # ─── 전체 통합 에이전트 macOS 빌드 ──────────────────────────────────────
  build-all-macos:
    runs-on: macos-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: pip 패키지 설치 (전체)
        env:
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: "1"
        run: |
          pip install pyinstaller
          pip install -r backend/requirements.txt
          pip install fastapi uvicorn[standard] pydantic undetected-chromedriver selenium python-dotenv yt-dlp

      - name: macOS 통합 PKG 빌드
        timeout-minutes: 35
        working-directory: installer
        run: |
          chmod +x build_all_macos.sh
          ./build_all_macos.sh

      - name: 아티팩트 업로드
        uses: actions/upload-artifact@v4
        with:
          name: all-macos-installer
          path: installer/dist/TubeMetric-All-Agents-Setup-macOS.pkg
          retention-days: 7

  # ─── TikTok Scraper Windows .exe 빌드 ───────────────────────────────────
  build-tiktok-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: pip 패키지 설치
        run: |
          pip install pyinstaller
          pip install fastapi uvicorn[standard] pydantic undetected-chromedriver selenium python-dotenv

      - name: PyInstaller 빌드
        working-directory: installer
        run: pyinstaller tiktok_scraper.spec --clean

      - name: Inno Setup 인스톨러 빌드
        uses: Minionguyjpro/Inno-Setup-Action@v1.2.2
        with:
          path: installer/setup_tiktok.iss
          options: /O"installer/Output"

      - name: 아티팩트 업로드
        uses: actions/upload-artifact@v4
        with:
          name: tiktok-windows-installer
          path: installer/Output/TubeMetric-TikTok-Agent-Setup-Windows.exe
          retention-days: 7

  # ─── TikTok Scraper macOS .pkg 빌드 ─────────────────────────────────────
  build-tiktok-macos:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: pip 패키지 설치
        run: |
          pip install pyinstaller
          pip install fastapi uvicorn[standard] pydantic undetected-chromedriver selenium python-dotenv

      - name: macOS PKG 빌드
        timeout-minutes: 20
        working-directory: installer
        run: |
          chmod +x build_tiktok_macos.sh
          ./build_tiktok_macos.sh

      - name: 아티팩트 업로드
        uses: actions/upload-artifact@v4
        with:
          name: tiktok-macos-installer
          path: installer/dist/TubeMetric-TikTok-Agent-Setup-macOS.pkg
          retention-days: 7

  # ─── GitHub Release에 인스톨러 배포 ──────────────────────────────────────
  release:
    needs: [build-all-windows, build-all-macos, build-windows, build-macos, build-softc-windows, build-softc-macos, build-instagram-windows, build-instagram-macos, build-tiktok-windows, build-tiktok-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: 파일 목록 확인
        run: find artifacts -type f

      - name: GitHub Release 생성 / 업데이트
        uses: softprops/action-gh-release@v2
        with:
          tag_name: local-agent-latest
          name: "TubeMetric Local Agent (최신)"
          body: |
            ## ⭐ 통합 설치 (권장) — 라이브 지표 + Instagram + TikTok 한 번에

            모든 로컬 에이전트를 한 번의 설치로 구성합니다.
            Python 및 모든 패키지 내장 — 별도 설치 불필요.

            | OS | 파일 |
            |---|---|
            | Windows 10/11 | `TubeMetric-All-Agents-Setup-Windows.exe` |
            | macOS (Intel/Apple Silicon) | `TubeMetric-All-Agents-Setup-macOS.pkg` |

            설치 후 Windows 시작 시 자동 실행:
            - **라이브 지표 에이전트** → 포트 8001
            - **Instagram·TikTok 에이전트** → 포트 8003

            ---

            ## TubeMetric Local Agent 설치 방법

            1. 아래에서 본인 OS에 맞는 파일을 다운로드하세요
            2. 실행 후 "다음" 클릭으로 설치 완료
            3. TubeMetric 사이트에서 라이브 지표 수집이 가능해집니다

            | OS | 파일 |
            |---|---|
            | Windows 10/11 | `TubeMetric-Agent-Setup-Windows.exe` |
            | macOS (Intel/Apple Silicon) | `TubeMetric-Agent-Setup-macOS.pkg` |

            > 설치 후 첫 실행 시 Playwright Chromium (~150MB)을 자동으로 다운로드합니다.

            ---

            ## TubeMetric SoftC Scraper (로컬 에이전트) 설치 방법

            1. 아래에서 본인 OS에 맞는 파일을 다운로드하세요
            2. 실행 후 "다음" 클릭으로 설치 완료
            3. **별도 설치 불필요** — 실행 즉시 포트 8002에서 서버 시작
            4. TubeMetric 사이트의 [라이브 지표 분석] → [로컬 에이전트] 탭에서 수집 가능

            | OS | 파일 |
            |---|---|
            | Windows 10/11 | `TubeMetric-SoftC-Scraper-Setup-Windows.exe` |
            | macOS (Intel/Apple Silicon) | `TubeMetric-SoftC-Scraper-Setup-macOS.pkg` |

            > PC에 Chrome이 설치되어 있어야 합니다 (Chrome 자동 감지).
            > headless=False · undetected_chromedriver 기반 bot 감지 우회 수집

            ---

            ## TubeMetric Instagram Scraper (로컬 에이전트) 설치 방법

            1. 아래에서 본인 OS에 맞는 파일을 다운로드하세요
            2. 실행 후 "다음" 클릭으로 설치 완료
            3. **별도 설치 불필요** — 실행 즉시 포트 8003에서 서버 시작
            4. TubeMetric 사이트의 [Instagram 분석] 탭에서 로컬 에이전트 모드로 수집 가능

            | OS | 파일 |
            |---|---|
            | Windows 10/11 | `TubeMetric-Instagram-Agent-Setup-Windows.exe` |
            | macOS (Intel/Apple Silicon) | `TubeMetric-Instagram-Agent-Setup-macOS.pkg` |

            > PC에 Chrome이 설치되어 있어야 합니다.
            > GitHub 토큰 없이 즉시 수집 가능

            ---

            ## TubeMetric TikTok Scraper (로컬 에이전트) 설치 방법

            1. 아래에서 본인 OS에 맞는 파일을 다운로드하세요
            2. 실행 후 "다음" 클릭으로 설치 완료
            3. **별도 설치 불필요** — 실행 즉시 포트 8004에서 서버 시작
            4. TubeMetric 사이트의 [TikTok 분석] 탭에서 로컬 에이전트 모드로 수집 가능

            | OS | 파일 |
            |---|---|
            | Windows 10/11 | `TubeMetric-TikTok-Agent-Setup-Windows.exe` |
            | macOS (Intel/Apple Silicon) | `TubeMetric-TikTok-Agent-Setup-macOS.pkg` |

            > PC에 Chrome이 설치되어 있어야 합니다.
            > 고정됨(Pinned) 영상 제외 평균 조회수 계산
          files: |
            artifacts/all-windows-installer/TubeMetric-All-Agents-Setup-Windows.exe
            artifacts/all-macos-installer/TubeMetric-All-Agents-Setup-macOS.pkg
            artifacts/windows-installer/TubeMetric-Agent-Setup-Windows.exe
            artifacts/macos-installer/TubeMetric-Agent-Setup-macOS.pkg
            artifacts/softc-windows-installer/TubeMetric-SoftC-Scraper-Setup-Windows.exe
            artifacts/softc-macos-installer/TubeMetric-SoftC-Scraper-Setup-macOS.pkg
            artifacts/instagram-windows-installer/TubeMetric-Instagram-Agent-Setup-Windows.exe
            artifacts/instagram-macos-installer/TubeMetric-Instagram-Agent-Setup-macOS.pkg
            artifacts/tiktok-windows-installer/TubeMetric-TikTok-Agent-Setup-Windows.exe
            artifacts/tiktok-macos-installer/TubeMetric-TikTok-Agent-Setup-macOS.pkg
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
